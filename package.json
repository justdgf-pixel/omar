{
  "name": "digisouk-dz",
  "private": true,
  "scripts": {
    "dev:web": "npm --prefix apps/web run dev",
    "build:web": "npm --prefix apps/web run build",
    "lint:web": "npm --prefix apps/web run lint",
    "start:mobile": "npm --prefix apps/mobile run start",
    "typecheck:mobile": "./apps/mobile/node_modules/.bin/tsc --noEmit -p apps/mobile/tsconfig.json",
    "verify": "npm run lint:web && npm run build:web && npm run typecheck:mobile"
  }
}
